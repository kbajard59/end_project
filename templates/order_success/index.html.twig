{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande - Burger Queen{% endblock %}

{% block content %}
    <h2>Confirmation de commande</h2>

    Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>,<br/><br/>
    Nous vous remercions pour votre commande n°<strong>{{ order.reference }}</strong>.<br/>
    Une confirmation vient de vous être envoyée par email à l'adresse <strong>{{ order.user.email }}</strong>.
    <hr>
    {% if order.deliveryName == 'Livraison à domicile' %}
        Votre commande sera livrée par <strong>{{ order.carrierName }}</strong> à l'adresse suivante : <br/>
        {{ order.delivery|raw }}
    {% else %}
        Votre commande sera disponible dans le resteurant que vous avez sélectionné dans quelques minutes !
    {% endif %}
    <hr>
    Pour suivre votre commande, rendez-vous dans votre <a href="{{{ path('account_order') }}}">compte</a>.

{% endblock %}
