{% extends 'base.html.twig' %}

{% block title %}Inscription - Burger Queen{% endblock %}

{% block content %}
    <h1>Inscription</h1>
    {% if notification %}
        <p class="alert alert-info">{{ notification }}</p>
    {% endif %}
    <div class="block-small">
        {{ form(form) }}
    </div>
{% endblock %}
